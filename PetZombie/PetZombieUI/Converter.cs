//using System;
using CocosSharp;
using PetZombie;

namespace PetZombieUI
{
    public static class Converter
    {
        static Converter()
        {
        }

        public static Position PointToPosition(CCPoint point)
        {
            // Some code.

                //var x = blockWidth * 0.5f + i * blockWidth;
                //var y = blockWidth * 0.5f + j * blockWidth;

            return null;
        }

        public static CCPoint PositionToPoint(Position position)
        {
            return default(CCPoint);
        }
    }
}

